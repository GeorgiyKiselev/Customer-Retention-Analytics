The ``p_value_threshold`` is set at 0.05. Even under the null hypothesis of no violations, some
covariates will be below the threshold by chance. This is compounded when there are many covariates.
Similarly, when there are lots of observations, even minor deviances from the proportional hazard
assumption will be flagged.

With that in mind, it's best to use a combination of statistical tests and visual tests to determine
the most serious violations. Produce visual plots using ``check_assumptions(..., show_plots=True)``
and looking for non-constant lines. See link [A] below for a full example.

<IPython.core.display.HTML object>


1. Variable 'recency' failed the non-proportional test: p-value is <5e-05.

   Advice 1: the functional form of the variable 'recency' might be incorrect. That is, there may be
non-linear terms missing. The proportional hazard test used is very sensitive to incorrect
functional forms. See documentation in link [D] below on how to specify a functional form.

   Advice 2: try binning the variable 'recency' using pd.cut, and then specify it in
`strata=['recency', ...]` in the call in `.fit`. See documentation in link [B] below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


2. Variable 'frequency' failed the non-proportional test: p-value is <5e-05.

   Advice 1: the functional form of the variable 'frequency' might be incorrect. That is, there may
be non-linear terms missing. The proportional hazard test used is very sensitive to incorrect
functional forms. See documentation in link [D] below on how to specify a functional form.

   Advice 2: try binning the variable 'frequency' using pd.cut, and then specify it in
`strata=['frequency', ...]` in the call in `.fit`. See documentation in link [B] below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


3. Variable 'average_interpurchase_time' failed the non-proportional test: p-value is <5e-05.

   Advice 1: the functional form of the variable 'average_interpurchase_time' might be incorrect.
That is, there may be non-linear terms missing. The proportional hazard test used is very sensitive
to incorrect functional forms. See documentation in link [D] below on how to specify a functional
form.

   Advice 2: try binning the variable 'average_interpurchase_time' using pd.cut, and then specify it
in `strata=['average_interpurchase_time', ...]` in the call in `.fit`. See documentation in link [B]
below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


4. Variable 'avg_interpurchase_missing' failed the non-proportional test: p-value is 0.0002.

   Advice: with so few unique values (only 2), you can include `strata=['avg_interpurchase_missing',
...]` in the call in `.fit`. See documentation in link [E] below.

5. Variable 'total_orders' failed the non-proportional test: p-value is <5e-05.

   Advice 1: the functional form of the variable 'total_orders' might be incorrect. That is, there
may be non-linear terms missing. The proportional hazard test used is very sensitive to incorrect
functional forms. See documentation in link [D] below on how to specify a functional form.

   Advice 2: try binning the variable 'total_orders' using pd.cut, and then specify it in
`strata=['total_orders', ...]` in the call in `.fit`. See documentation in link [B] below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


6. Variable 'orders_last_7d' failed the non-proportional test: p-value is <5e-05.

   Advice 1: the functional form of the variable 'orders_last_7d' might be incorrect. That is, there
may be non-linear terms missing. The proportional hazard test used is very sensitive to incorrect
functional forms. See documentation in link [D] below on how to specify a functional form.

   Advice 2: try binning the variable 'orders_last_7d' using pd.cut, and then specify it in
`strata=['orders_last_7d', ...]` in the call in `.fit`. See documentation in link [B] below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


7. Variable 'orders_last_14d' failed the non-proportional test: p-value is <5e-05.

   Advice 1: the functional form of the variable 'orders_last_14d' might be incorrect. That is,
there may be non-linear terms missing. The proportional hazard test used is very sensitive to
incorrect functional forms. See documentation in link [D] below on how to specify a functional form.

   Advice 2: try binning the variable 'orders_last_14d' using pd.cut, and then specify it in
`strata=['orders_last_14d', ...]` in the call in `.fit`. See documentation in link [B] below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


8. Variable 'orders_last_30d' failed the non-proportional test: p-value is <5e-05.

   Advice 1: the functional form of the variable 'orders_last_30d' might be incorrect. That is,
there may be non-linear terms missing. The proportional hazard test used is very sensitive to
incorrect functional forms. See documentation in link [D] below on how to specify a functional form.

   Advice 2: try binning the variable 'orders_last_30d' using pd.cut, and then specify it in
`strata=['orders_last_30d', ...]` in the call in `.fit`. See documentation in link [B] below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


9. Variable 'orders_last_60d' failed the non-proportional test: p-value is <5e-05.

   Advice 1: the functional form of the variable 'orders_last_60d' might be incorrect. That is,
there may be non-linear terms missing. The proportional hazard test used is very sensitive to
incorrect functional forms. See documentation in link [D] below on how to specify a functional form.

   Advice 2: try binning the variable 'orders_last_60d' using pd.cut, and then specify it in
`strata=['orders_last_60d', ...]` in the call in `.fit`. See documentation in link [B] below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


10. Variable 'orders_first_30d' failed the non-proportional test: p-value is <5e-05.

   Advice 1: the functional form of the variable 'orders_first_30d' might be incorrect. That is,
there may be non-linear terms missing. The proportional hazard test used is very sensitive to
incorrect functional forms. See documentation in link [D] below on how to specify a functional form.

   Advice 2: try binning the variable 'orders_first_30d' using pd.cut, and then specify it in
`strata=['orders_first_30d', ...]` in the call in `.fit`. See documentation in link [B] below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


11. Variable 'monetary_first_30d' failed the non-proportional test: p-value is 0.0181.

   Advice 1: the functional form of the variable 'monetary_first_30d' might be incorrect. That is,
there may be non-linear terms missing. The proportional hazard test used is very sensitive to
incorrect functional forms. See documentation in link [D] below on how to specify a functional form.

   Advice 2: try binning the variable 'monetary_first_30d' using pd.cut, and then specify it in
`strata=['monetary_first_30d', ...]` in the call in `.fit`. See documentation in link [B] below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


12. Variable 'frequency_trend_30d_vs_prior' failed the non-proportional test: p-value is <5e-05.

   Advice 1: the functional form of the variable 'frequency_trend_30d_vs_prior' might be incorrect.
That is, there may be non-linear terms missing. The proportional hazard test used is very sensitive
to incorrect functional forms. See documentation in link [D] below on how to specify a functional
form.

   Advice 2: try binning the variable 'frequency_trend_30d_vs_prior' using pd.cut, and then specify
it in `strata=['frequency_trend_30d_vs_prior', ...]` in the call in `.fit`. See documentation in
link [B] below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


13. Variable 'days_since_first_purchase' failed the non-proportional test: p-value is <5e-05.

   Advice 1: the functional form of the variable 'days_since_first_purchase' might be incorrect.
That is, there may be non-linear terms missing. The proportional hazard test used is very sensitive
to incorrect functional forms. See documentation in link [D] below on how to specify a functional
form.

   Advice 2: try binning the variable 'days_since_first_purchase' using pd.cut, and then specify it
in `strata=['days_since_first_purchase', ...]` in the call in `.fit`. See documentation in link [B]
below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


14. Variable 'days_since_last_purchase' failed the non-proportional test: p-value is <5e-05.

   Advice 1: the functional form of the variable 'days_since_last_purchase' might be incorrect. That
is, there may be non-linear terms missing. The proportional hazard test used is very sensitive to
incorrect functional forms. See documentation in link [D] below on how to specify a functional form.

   Advice 2: try binning the variable 'days_since_last_purchase' using pd.cut, and then specify it
in `strata=['days_since_last_purchase', ...]` in the call in `.fit`. See documentation in link [B]
below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


15. Variable 'purchase_acceleration' failed the non-proportional test: p-value is 0.0091.

   Advice 1: the functional form of the variable 'purchase_acceleration' might be incorrect. That
is, there may be non-linear terms missing. The proportional hazard test used is very sensitive to
incorrect functional forms. See documentation in link [D] below on how to specify a functional form.

   Advice 2: try binning the variable 'purchase_acceleration' using pd.cut, and then specify it in
`strata=['purchase_acceleration', ...]` in the call in `.fit`. See documentation in link [B] below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


16. Variable 'purchase_gap_std' failed the non-proportional test: p-value is <5e-05.

   Advice 1: the functional form of the variable 'purchase_gap_std' might be incorrect. That is,
there may be non-linear terms missing. The proportional hazard test used is very sensitive to
incorrect functional forms. See documentation in link [D] below on how to specify a functional form.

   Advice 2: try binning the variable 'purchase_gap_std' using pd.cut, and then specify it in
`strata=['purchase_gap_std', ...]` in the call in `.fit`. See documentation in link [B] below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


17. Variable 'purchase_gap_cv' failed the non-proportional test: p-value is <5e-05.

   Advice 1: the functional form of the variable 'purchase_gap_cv' might be incorrect. That is,
there may be non-linear terms missing. The proportional hazard test used is very sensitive to
incorrect functional forms. See documentation in link [D] below on how to specify a functional form.

   Advice 2: try binning the variable 'purchase_gap_cv' using pd.cut, and then specify it in
`strata=['purchase_gap_cv', ...]` in the call in `.fit`. See documentation in link [B] below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


18. Variable 'max_purchase_gap' failed the non-proportional test: p-value is <5e-05.

   Advice 1: the functional form of the variable 'max_purchase_gap' might be incorrect. That is,
there may be non-linear terms missing. The proportional hazard test used is very sensitive to
incorrect functional forms. See documentation in link [D] below on how to specify a functional form.

   Advice 2: try binning the variable 'max_purchase_gap' using pd.cut, and then specify it in
`strata=['max_purchase_gap', ...]` in the call in `.fit`. See documentation in link [B] below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


19. Variable 'min_purchase_gap' failed the non-proportional test: p-value is <5e-05.

   Advice 1: the functional form of the variable 'min_purchase_gap' might be incorrect. That is,
there may be non-linear terms missing. The proportional hazard test used is very sensitive to
incorrect functional forms. See documentation in link [D] below on how to specify a functional form.

   Advice 2: try binning the variable 'min_purchase_gap' using pd.cut, and then specify it in
`strata=['min_purchase_gap', ...]` in the call in `.fit`. See documentation in link [B] below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


20. Variable 'active_purchase_span_days' failed the non-proportional test: p-value is <5e-05.

   Advice 1: the functional form of the variable 'active_purchase_span_days' might be incorrect.
That is, there may be non-linear terms missing. The proportional hazard test used is very sensitive
to incorrect functional forms. See documentation in link [D] below on how to specify a functional
form.

   Advice 2: try binning the variable 'active_purchase_span_days' using pd.cut, and then specify it
in `strata=['active_purchase_span_days', ...]` in the call in `.fit`. See documentation in link [B]
below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


21. Variable 'unique_purchase_days' failed the non-proportional test: p-value is <5e-05.

   Advice 1: the functional form of the variable 'unique_purchase_days' might be incorrect. That is,
there may be non-linear terms missing. The proportional hazard test used is very sensitive to
incorrect functional forms. See documentation in link [D] below on how to specify a functional form.

   Advice 2: try binning the variable 'unique_purchase_days' using pd.cut, and then specify it in
`strata=['unique_purchase_days', ...]` in the call in `.fit`. See documentation in link [B] below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


22. Variable 'orders_per_active_day' failed the non-proportional test: p-value is <5e-05.

   Advice 1: the functional form of the variable 'orders_per_active_day' might be incorrect. That
is, there may be non-linear terms missing. The proportional hazard test used is very sensitive to
incorrect functional forms. See documentation in link [D] below on how to specify a functional form.

   Advice 2: try binning the variable 'orders_per_active_day' using pd.cut, and then specify it in
`strata=['orders_per_active_day', ...]` in the call in `.fit`. See documentation in link [B] below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


23. Variable 'unique_categories' failed the non-proportional test: p-value is <5e-05.

   Advice 1: the functional form of the variable 'unique_categories' might be incorrect. That is,
there may be non-linear terms missing. The proportional hazard test used is very sensitive to
incorrect functional forms. See documentation in link [D] below on how to specify a functional form.

   Advice 2: try binning the variable 'unique_categories' using pd.cut, and then specify it in
`strata=['unique_categories', ...]` in the call in `.fit`. See documentation in link [B] below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


24. Variable 'unique_products' failed the non-proportional test: p-value is <5e-05.

   Advice 1: the functional form of the variable 'unique_products' might be incorrect. That is,
there may be non-linear terms missing. The proportional hazard test used is very sensitive to
incorrect functional forms. See documentation in link [D] below on how to specify a functional form.

   Advice 2: try binning the variable 'unique_products' using pd.cut, and then specify it in
`strata=['unique_products', ...]` in the call in `.fit`. See documentation in link [B] below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


25. Variable 'repeat_category_rate' failed the non-proportional test: p-value is 0.0001.

   Advice 1: the functional form of the variable 'repeat_category_rate' might be incorrect. That is,
there may be non-linear terms missing. The proportional hazard test used is very sensitive to
incorrect functional forms. See documentation in link [D] below on how to specify a functional form.

   Advice 2: try binning the variable 'repeat_category_rate' using pd.cut, and then specify it in
`strata=['repeat_category_rate', ...]` in the call in `.fit`. See documentation in link [B] below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


26. Variable 'dominant_category_share' failed the non-proportional test: p-value is 0.0013.

   Advice 1: the functional form of the variable 'dominant_category_share' might be incorrect. That
is, there may be non-linear terms missing. The proportional hazard test used is very sensitive to
incorrect functional forms. See documentation in link [D] below on how to specify a functional form.

   Advice 2: try binning the variable 'dominant_category_share' using pd.cut, and then specify it in
`strata=['dominant_category_share', ...]` in the call in `.fit`. See documentation in link [B]
below.

   Advice 3: try adding an interaction term with your time variable. See documentation in link [C]
below.


---
[A]  https://lifelines.readthedocs.io/en/latest/jupyter_notebooks/Proportional%20hazard%20assumption.html
[B]  https://lifelines.readthedocs.io/en/latest/jupyter_notebooks/Proportional%20hazard%20assumption.html#Bin-variable-and-stratify-on-it
[C]  https://lifelines.readthedocs.io/en/latest/jupyter_notebooks/Proportional%20hazard%20assumption.html#Introduce-time-varying-covariates
[D]  https://lifelines.readthedocs.io/en/latest/jupyter_notebooks/Proportional%20hazard%20assumption.html#Modify-the-functional-form
[E]  https://lifelines.readthedocs.io/en/latest/jupyter_notebooks/Proportional%20hazard%20assumption.html#Stratification
